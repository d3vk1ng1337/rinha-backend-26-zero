x-worker: &worker
  image: ghcr.io/d3vk1ng1337/rinha-backend-26-zero:d6daf7c2aa8ea4a96ce13aeb5464b7da9e494b2c
  command: ["worker"]
  environment: &workerenv
    INDEX_PATH: /index/index.bin
    NPROBE: "12"
    REPAIR_MIN: "1"
    REPAIR_MAX: "4"
    BUSY_POLL_US: "0"
    EPOLL_SPIN_US: "0"
    EPOLL_IDLE_US: "40"
    MALLOC_ARENA_MAX: "1"
  volumes:
    - socks:/run/sock
  ulimits:
    nofile:
      soft: 65535
      hard: 65535

services:
  worker0:
    <<: *worker
    cpuset: "0"
    environment:
      <<: *workerenv
      CTRL: /run/sock/api0.ctrl
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: 160M

  worker1:
    <<: *worker
    cpuset: "1"
    environment:
      <<: *workerenv
      CTRL: /run/sock/api1.ctrl
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: 160M

  lb:
    image: ghcr.io/d3vk1ng1337/rinha-backend-26-zero:d6daf7c2aa8ea4a96ce13aeb5464b7da9e494b2c
    command: ["lb"]
    cpuset: "2,3"
    environment:
      PORT: "9999"
      BACKENDS: "/run/sock/api0.ctrl,/run/sock/api1.ctrl"
    ports:
      - "9999:9999"
    volumes:
      - socks:/run/sock
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    depends_on:
      - worker0
      - worker1
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 30M

volumes:
  socks:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "size=10m"
