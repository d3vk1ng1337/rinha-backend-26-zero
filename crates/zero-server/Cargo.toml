[package]
name = "zero-server"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "zero-server"
path = "src/main.rs"

[dependencies]
zero-index = { path = "../zero-index" }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
