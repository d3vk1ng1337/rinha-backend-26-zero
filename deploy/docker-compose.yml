x-worker: &worker
  build:
    context: ..
    dockerfile: deploy/Dockerfile
  command: ["worker"]
  volumes:
    - socks:/run/sock
  ulimits:
    nofile:
      soft: 65535
      hard: 65535

services:
  worker0:
    <<: *worker
    cpuset: "0"
    environment:
      CTRL: /run/sock/api0.ctrl
      INDEX_PATH: /index/index.bin
      NPROBE: "12"
      REPAIR_MIN: "1"
      REPAIR_MAX: "4"
      BUSY_POLL_US: "${BUSY_POLL_US:-100}"
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: 165M

  worker1:
    <<: *worker
    cpuset: "1"
    environment:
      CTRL: /run/sock/api1.ctrl
      INDEX_PATH: /index/index.bin
      NPROBE: "12"
      REPAIR_MIN: "1"
      REPAIR_MAX: "4"
      BUSY_POLL_US: "${BUSY_POLL_US:-100}"
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: 165M

  lb:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command: ["lb"]
    cpuset: "2,3"
    environment:
      PORT: "9999"
      BACKENDS: "/run/sock/api0.ctrl,/run/sock/api1.ctrl"
    ports:
      - "${HOST_PORT:-9999}:9999"
    volumes:
      - socks:/run/sock
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    depends_on:
      - worker0
      - worker1
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: 20M

volumes:
  socks:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "size=10m"
